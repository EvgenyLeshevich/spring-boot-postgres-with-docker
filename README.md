## How to run this?
```bash
$ git clone https://github.com/EvgenyLeshevich/spring-boot-postgres-with-docker.git
$ cd docker-spring-boot
$ mvn clean package -Dmaven.test.skip=true

//dockerize

$ sudo docker-compose up -d

  access http://localhost:8080
```
